# Intelligence Brief Backtest Linkage

The brief-to-backtest link preserves the deterministic run ID plus input and output hashes. Its
relationship is `historical_research_evidence`.

The contract fixes `evidence_permission` and `strategy_promotion` to false. Historical results
remain evidence, not authority.
