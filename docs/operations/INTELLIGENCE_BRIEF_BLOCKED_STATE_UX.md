# Intelligence Brief Blocked-State UX

The local case selector presents three states:

- `available`: validated brief detail remains visible.
- `blocked`: evidence reason remains visible while scenarios are suppressed.
- `unavailable`: no brief detail is shown.

The availability region is a live status message. It does not contain a command, alert,
notification, or execution control.
