# Intelligence Brief Case Selection

Case selection is manual and local. The selector contains validated options with an availability
status and reason. Changing the selection only changes which checked-in evidence is visible in the
browser.

Selection does not fetch, generate, schedule, notify, rank, simulate, or execute.
