# Intelligence Brief Source Provenance

The workspace derives source title, repository-relative path, freshness, as-of timestamp, payload
hash, provenance status, notes, and limitations from validated local source envelopes.

No provider URL, remote lookup, credential path, or external source expansion is present.
