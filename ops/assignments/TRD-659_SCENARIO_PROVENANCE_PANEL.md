# TRD-659 Scenario Provenance Panel

Status: accepted

## Goal

Keep the local sources supporting the simulator handoff visible beside the case.

## Acceptance

Only checked-in `ops/` and `docs/` provenance references are accepted and rendered.
