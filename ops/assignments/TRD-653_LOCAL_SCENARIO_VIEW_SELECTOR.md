# TRD-653 Local Scenario View Selector

Status: accepted

## Goal

Allow the operator to switch among checked-in evidence fixtures without running a simulation.

## Acceptance

The selector uses buttons with accurate `aria-pressed` state and performs display changes only.
