# GateZero Core

This package contains the protected decision-loop ordering model.

It keeps strategy review work aligned to:

```text
Strategy Idea -> Data Snapshot -> Backtest -> Metric Report -> Risk Review -> Operator Decision -> Outcome Logged -> Learning Event
```

The package is Gate 0 Research Only and does not execute trades or interact with brokers.
