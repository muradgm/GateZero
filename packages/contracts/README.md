# GateZero Contracts

This package defines runtime-validated contracts for the Gate 0 Research-Only decision loop.

The contracts model research evidence and risk review. They do not create execution capability,
broker connectivity, autonomous execution, or AI buy/sell prediction.

Every contract exports a TypeScript type and a Zod schema so callers can validate data at runtime.
