interface:
  display_name: "GateZero Quant Backtest Reviewer"
  short_description: "GateZero backtest contract review"
  default_prompt:
    "Use $gatezero-quant-backtest-reviewer to review GateZero backtest contracts, assumptions,
    reproducibility, metric integrity, and claim safety."

policy:
  allow_implicit_invocation: false
