interface:
  display_name: "GateZero QA Security Reviewer"
  short_description: "GateZero validation and security review"
  default_prompt:
    "Use $gatezero-qa-security-reviewer to review GateZero validation integrity, scanner coverage,
    secrets exposure, blocked-scope checks, and QA evidence."

policy:
  allow_implicit_invocation: false
