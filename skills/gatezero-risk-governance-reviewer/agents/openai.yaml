interface:
  display_name: "GateZero Risk Governance Reviewer"
  short_description: "GateZero risk and autonomy gate review"
  default_prompt:
    "Use $gatezero-risk-governance-reviewer to review GateZero risk gates, autonomy gates,
    approval-language blockers, and phase-safe scope."

policy:
  allow_implicit_invocation: false
