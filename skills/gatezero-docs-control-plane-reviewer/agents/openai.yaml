interface:
  display_name: "GateZero Docs Control Plane Reviewer"
  short_description: "GateZero docs and tracker review"
  default_prompt:
    "Use $gatezero-docs-control-plane-reviewer to review GateZero docs, tracker, source links,
    handoff records, and control-plane freshness."

policy:
  allow_implicit_invocation: false
