interface:
  display_name: "Trader Product Reviewer"
  short_description: "GateZero trading UX and trust review"
  default_prompt:
    "Use $trader-product-reviewer to review this GateZero surface for evidence clarity, risk
    visibility, workflow trust, and phase-safe product scope."

policy:
  allow_implicit_invocation: false
