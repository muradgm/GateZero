interface:
  display_name: "GateZero Orchestrator Reviewer"
  short_description: "GateZero assignment and scope review"
  default_prompt:
    "Use $gatezero-orchestrator-reviewer to review GateZero assignment sequencing, scope boundaries,
    acceptance criteria, and next-agent handoff."

policy:
  allow_implicit_invocation: false
