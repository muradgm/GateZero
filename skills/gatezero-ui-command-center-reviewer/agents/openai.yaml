interface:
  display_name: "GateZero UI Command Center Reviewer"
  short_description: "GateZero read-only UI review"
  default_prompt:
    "Use $gatezero-ui-command-center-reviewer to review GateZero command-center UI for read-only
    evidence clarity, boundary copy, source links, and accessibility."

policy:
  allow_implicit_invocation: false
